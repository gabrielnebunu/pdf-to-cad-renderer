# Core PDF processing
PyMuPDF==1.24.5          # fitz — PDF → high-res raster
pdf2image==1.17.0         # alternative raster extraction

# Computer Vision
opencv-python==4.9.0.80   # preprocessing: threshold, denoise, Hough lines
numpy==1.26.4
Pillow==10.3.0
scipy==1.13.0             # skimage deskew helpers

# Vectorization
# potracer==0.0.4  <- correct version (no 1.0.7 exists), see vectorizer.py
potracer==0.0.4           # pure Python potrace — raster to SVG
svgpathtools==1.6.1       # parse/manipulate SVG paths
cairosvg==2.7.1           # SVG ↔ PDF/PNG conversion

# CAD output
ezdxf==1.3.4              # generate/modify DXF files (MIT)

# API
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9   # file uploads

# Utils
python-dotenv==1.0.1
loguru==0.7.2
